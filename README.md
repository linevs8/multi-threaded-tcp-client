# Multi-threaded TCP Client and Shannon-Fano Encoding Server

This project includes a multi-threaded TCP client that connects to a server to encode messages using Shannon-Fano compression.

## Features
- POSIX-threaded client reads multiple messages and connects to server concurrently
- Server
